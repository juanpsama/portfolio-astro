
---
import { Image } from "astro:assets"
import ProfileImage from '../../public/face.jpg';
import { getLangFromUrl, useTranslatedPath, useTranslations} from '../i18n/utils';
import LanguagePicker from "./LanguagePicker.astro";


const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const translatePath = useTranslatedPath(lang);

function useBasePath(path:string){
    return `${import.meta.env.BASE_URL}${translatePath(path)}`;
}
---
<nav>
        <div class="flex flex-col md:flex-row justify-center items-center">
            
            <div class="h-40 w-40">
                <Image src={ProfileImage} alt='Dont Work' width = 400 height=400 
                loading="eager" class= "rounded-full h-full w-full object-cover"/>
            </div>
    
            <div class="block px-10">
                <span>
                    <h2 class="text-primary text-primary-glow" ><b>Juan P. Samayoa</b></h2>
                    <h5 class="mx-3 text-primary text-primary-glow">{t('subtitle')}</h5>
                </span>
        
                <div class="inline-flex text-secondary ">
                    <a class="mx-3 hover:text-primary hover:underline decoration-2 decoration-primary" aria-current="page" href={useBasePath('/')}>{t('nav.home')}</a>
                    <a class="mx-3 hover:text-primary hover:underline decoration-2 decoration-primary" href={useBasePath('/projects/')}>{t('nav.projects')}</a>
                    <a class="mx-3 hover:text-primary hover:underline decoration-2 decoration-primary" href={useBasePath('/contact/')} >{t('nav.contact')}</a>
                </div>
        
            </div>
        </div>
    </nav>