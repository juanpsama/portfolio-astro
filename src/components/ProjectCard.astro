---
import { Icon } from 'astro-icon/components'
interface Props {
	title: string;
	image: string;
  github:string;
  url : string;
  description ?: string;
  tags ?: Array<String>;
}
const { image, title, 
      description, tags,
       github, url} = Astro.props;

       
---
<div class="relative rounded-lg shadow-soft bg-elevated text-primary">

    <img class="w-full rounded-t-lg" src={`${import.meta.env.BASE_URL}${image}`} alt="Project image">
    
    <div class="px-6 py-4 mb-10" >
      <div class="font-bold text-xl mb-2">{title}</div>

      {tags && tags.map(tag => (
        <div class="rounded-full chip-primary inline-block mx-1 px-2 py-0.5 text-xs">{tag}</div>
      ))}

      <p class="text-secondary text-base leading-normal" set:html = {description}></p>
    </div>

    <div class="absolute inset-x-0 bottom-0 px-6 py-4 flex justify-center items-end">

        { url !== '' &&
        <a class="btn-primary font-bold rounded-full mx-2" href={url}>
            <Icon name="mdi:play" class="text-4xl"/>  
        </a>
        }

        { github !== '' &&
        <a class="btn-primary font-bold rounded-full mx-2" href={github}>
          <Icon name="mdi:github" class="text-4xl " />
        </a>
        }

    </div>

  </div>
  